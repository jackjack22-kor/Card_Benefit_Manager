# 변경 이력 (Changelog)

사람이 읽는 변경 요약입니다. 상세 diff는 git 로그를 참고하세요.
**새 작업을 이어서 할 때는 이 파일 최상단에 최신 변경을 추가**해 항상 최신 기준을 유지하세요. (Codex/Claude 공용 기준 문서)

## 2026-07-04

### 월별 실적 저장 안정화 · 혜택 한도/자동계산 보강
- **월별 실적 값이 화면 이동 후 예전 값으로 되돌아갈 수 있는 동기화 타이밍 보정**: `saveState()`가 실제 저장된 `updatedAt` 포함 상태를 반환하도록 하고, 데이터 변경(`persistState`) 시 메모리 state에도 반영해 클라우드 저장 큐가 최신 타임스탬프를 사용하도록 수정. 반대로 탭 이동·월 이동·테마·상세설정 열림 같은 보기 상태는 `touch:false`로 저장해 데이터 병합 기준을 흔들지 않게 분리.
- **신한 The ACE 블루라벨 의료비 연 20만원 한도 반영**: 기존 데이터 정의에는 `annualCap: 200000`이 있었지만 추천 계산은 월 한도만 반영. `amount_cap`/`amount_cap_pool`/`reward_cap_pool` 추천 계산에 연간 잔여 한도를 반영하고, 연한도 소진 시 조건 힌트로 표시.
- **횟수형 혜택 연간 횟수 제한 반영**: `count_amount` 추천 계산에서 월 횟수뿐 아니라 연 횟수(`annualLimitCount`) 소진도 체크. 월 금액 한도도 기존 사용 혜택액을 차감해 남은 한도 기준으로 계산.
- **이번달 사용금액 입력 시 혜택액 자동계산**: 금액한도형 혜택에서 `이번달 사용금액`을 입력하면 `혜택 사용액/적립액`을 혜택률·월한도·연한도·통합한도 기준으로 자동 산출. 예: 더에이스 의료비 50만원 입력 → 10% 5만원이 아니라 월 3만원/연 20만원 잔여분 내 금액으로 자동 제한.
- **카드상세 한도 표시 강화**: 연간 금액 한도가 있는 혜택은 요약 칩과 남은 한도 영역에 월한도/연한도를 함께 표시.

### 동기화 규칙 수정 · 하단 바 재구조화 · 카드상세 정리
- **클라우드 동기화를 '값 입력 시에만'으로 제한**: 기존엔 `setState`(탭 이동·월 이동·카테고리·카드 선택 등 UI 변경)도 전체 state를 클라우드에 기록해, 한 기기의 화면 이동이 다른 기기로 미러링됐음. ① `setState`/테마 토글/상세설정 토글은 **로컬 저장만**(동기화 안 함), 데이터 변경(실적·혜택·카드순서·포인트·백업/복원)만 `queueCloudSave`. ② 원격 반영(`applyRemoteState`)은 데이터만 반영하고 **화면 이동/보기 상태(`UI_STATE_KEYS`)·테마는 이 기기 값 유지** → 다른 기기 조작이 화면을 바꾸지 않음. (src/main.js)
- **하단 탭바를 `position: fixed` → `sticky` + flex 레이아웃으로 재구조화**: iOS에서 짧은 페이지일 때 fixed 바가 문서 끝으로 떠오르던 버그를 근본 차단. `.app-shell`을 100dvh flex 컬럼으로, `.view`를 `flex:1`로 두어 짧은 페이지는 본문이 공간을 채우고, `.tabs`는 `sticky bottom:0`로 항상 바닥 고정(긴 페이지 스크롤 시도 유지). 실측: 짧은/긴 페이지 모두 바가 뷰포트 바닥(gap 0).
- **설정 '앱/저장 안내' 상시 표시**(아코디언 → 일반 카드) + 동기화 정책 안내 문구 추가.
- **카드상세 큰 카드 이미지 제거**: 상단 썸네일 그리드로 카드를 고르므로 상세 히어로의 대형 카드 이미지는 중복 → 삭제해 상세를 컴팩트하게.

### 설정·상단·하단 바 정리
- **상단 타이틀 영역 높이 통일**: 카드현황/카드상세 page-head 높이가 미묘하게 달라 탭 전환 시 흔들리던 문제. `.head-top` `min-height: 44px`(스테퍼만/스테퍼+⋯ 정렬), 설명문 `line-clamp: 2 + min-height`로 2줄 고정 → 두 페이지 page-head 140px로 정확히 일치.
- **하단 탭 라벨 '설정/백업' → '설정'**, 설정 페이지 타이틀도 '설정'으로 변경.
- **클라우드 동기화 카드 재구성**: 좌측으로 쏠린 4줄 → 상태 배지 + '지금 동기화'(로그아웃 시 'Google 로그인')를 **동일 크기 2열 한 줄**(`.sync-row`)로. 계정 정보는 하단 나열 대신 **다른 메뉴처럼 우측 '+' 확장**(`.account-expand`, 아코디언 스타일)으로 통일.
- **하단 탭바 iOS 부유 버그 강화 조치**: 짧은 페이지에서 콘텐츠가 뷰포트보다 작아 `position:fixed` 바가 문서 끝에 뜨던 iOS 버그(안드로이드 정상)에, `.app-shell`에 `min-height: 100dvh`(100vh 폴백) 추가 → 문서가 항상 뷰포트를 채워 바가 바닥에 고정. (fixed 바 부유의 표준 해결책)

### 카드상세 성능·밀도 개선
- **카드 선택 시 부분 렌더로 깜빡임 제거**: 기존엔 카드 선택도 전체 `render()`(헤더·13개 썸네일·상세 전부 재생성)라 깜빡였음. `selectCard()`가 `.detail-main`만 교체하고 picker의 active 클래스만 토글하도록 변경(`renderCardDetailMain` 분리, detail-main 전용 `bindDetailMainEvents(root)` 스코프 바인딩). 헤더·썸네일·탭 DOM은 그대로 유지.
- **불필요한 클라우드 동기화 방지**: 카드 "선택"은 순수 화면 이동이라 `selectCard()`가 localStorage 저장·`queueCloudSave`를 하지 않음(기존 `setState`는 매 선택마다 클라우드 저장을 큐잉했음). 실제 데이터 입력은 기존대로 저장·동기화.
- **카드 타일 밀도 상향**: `minmax(64px)` → `minmax(52px)`로 조정해 모바일에서 약 6열 3줄로 전체 표시(화면폭 넓으면 열 수 자동 증가).

### 카드상세 UX 보완
- **카드 목록을 반응형 그리드로 교체**: 가로 스크롤 썸네일 스트립 → `repeat(auto-fill, minmax(64px,1fr))` 그리드(`.card-picker`, 이미지 타일). 전체 카드가 스크롤 없이 보여 (좁은 화면 여러 줄, 넓은 화면 한 줄) 화면폭에 따라 줄 수 자동 조절. 스크롤 위치 리셋·오프스크린 선택 미표시 문제 해소.
- **월 스테퍼 위치 통일**: 카드상세도 카드현황과 동일하게 상단 좌측(`head-top`)에 배치.
- **페이지 타이틀/설명 추가**: 카드상세 상단에 "카드 상세" 제목·설명을 넣어 다른 탭과 통일.
- **하단 탭바가 짧은 페이지(설정 등)에서 위로 뜨던 문제 수정**: iOS Safari의 `position: fixed` + `backdrop-filter` 동일 요소 조합 버그로, 콘텐츠가 짧으면 바가 바닥에 고정되지 않고 문서 끝에 위치. `.tabs`의 `backdrop-filter`(프로스티드 블러)를 제거하고 불투명 배경(`var(--surface)`)으로 변경해 고정 보장. (데스크톱 Chromium은 정상이라 미재현되던 버그)

### 카드 혜택 데이터 보강
- **월 실적 목표 기본값 보정**: 신한 Always On 월 목표를 1만원, KB 톡톡 my point 월 목표를 20만원으로 설정. 기존 저장 데이터에서 두 카드가 `관리 안 함(0원)`이면 schema `2.0.1` 마이그레이션으로 새 기본 목표를 1회 반영.
- **현대 아멕스 플래티넘 메탈 플레이트 항목 제거**: 이미 발급 완료된 일회성 안내 항목이라 카드상세 혜택 목록에서 삭제.
- **삼성 THE 1 AMEX PLATINUM ELITE 혜택 재정리**: `2604_AMEX_E_002.pdf` 기준으로 삼성카드 기본 혜택과 AMEX 등급서비스를 분리. AMEX 무료 주차, 호텔 발레파킹, 공항 발레파킹, 공항 라운지, 공항 커피, 공항 리무진·철도, 공항 Books Store 할인, AMEX 호텔/쇼핑/라이프스타일 우대 서비스를 확정 조건으로 보강. PDF 표에서 삼성 THE 1은 Be My Guest 미제공(`×`)이므로 THE 1에는 추가하지 않음.
- **결제추천 세부 매칭 보정**: 공항 커피 세부 카테고리를 추가하고, 주차 세부 선택 시 `인천공항/공항 발렛/호텔 발렛`이 섞이지 않도록 parking broad 매칭을 축소.
- **현대 아멕스 플래티넘 Amex Member 혜택 추가**: 아티제 커피 1+1, 백미당 커피·아이스크림 무료, Be My Guest 레스토랑/호텔 델리 할인, 국내 특급호텔·고택 호텔 할인, 국내 특급호텔 발레파킹, 공항 리무진·철도 무료, 공항 발레파킹, The Hotel Collection, Fine Hotels + Resorts를 추가. 카드상세 노출 순서는 사용 빈도 기준으로 아티제 → 백미당 → Be My Guest를 상단에 두고, 공항 리무진/발렛은 하단, THC/FHR은 최하단에 배치.
- **삼성 THE 1 누락 혜택 추가**: 삼성카드 앱 캡처 기준 무료 주차, SFC 놀이공원 우대, 발렛파킹, 메가박스 할인, 아웃백 10% 현장할인, 공항 커피, 공항 라운지 서비스를 추가. 금액 조건이 확인된 메가박스/아웃백은 결제추천에 반영하고, 대상처·조건 추가 확인이 필요한 공항/주차/라운지 서비스는 카드상세 관리 중심으로 반영.
- **세부 카테고리 보강**: 결제추천 정확도를 위해 커피(아티제·백미당), 호텔(FHR·THC·호텔 델리), 다이닝(아웃백·호텔 레스토랑·호텔 델리), 주차(공항/호텔 발렛) 세부 선택지를 추가.

### UX 개편 (레이아웃·정보 밀도)
- **하단 탭 순서 변경**: 카드현황 · 카드상세 · 결제추천 · 설정/백업.
- **헤더에서 월 이동 바 제거** → 헤더는 브랜드+테마토글만. 월은 카드현황/카드상세 상단의 **컴팩트 월 스테퍼**(`renderMonthStepper`, `‹ 2026년 7월 ›` + 이번달 아닐 때 '오늘')로 이동. 결제추천/설정에는 미표시.
- **카드현황 '정렬' → ⋯ 오버플로 메뉴**(`renderOverflowMenu`, `<details>` 드롭다운)로 이동해 공간 절약. 제목은 "카드 현황"으로.
- **요약 칩 재구성**: 좌측 `이번달 총 사용 금액`(전체 카드 currentMonthSpend 합계), 우측 `채울 실적 합계` = 금액 + 하단 `채운/관리 카드 수 달성`.
- **결제추천 카테고리/세부 카테고리 더보기**: 주요 7개(커피·대중교통·영화·백화점·의료·간편결제·해외)만 기본, 나머지는 더보기 확장. 세부는 상위 3개+더보기. (`categoryExpanded`/`subcategoryExpanded` 세션 유지)
- **카드상세 카드 목록 → 가로 썸네일 스트립**(`.card-strip`): 세로 목록 패널을 한 줄 스크롤 썸네일로 축소, 활성 카드 강조.
- **설정/백업 아코디언화**(`renderSettingsSection`, `<details>`): 포인트 가치·카드 순서·백업/복원·앱 안내를 접기. `백업 권장` 상시 문구 삭제. `데이터 이동 순서`는 백업/복원 하단으로 통합. **클라우드 동기화(코덱스)는 로직/핸들러 보존**하고 상태+지금 동기화만 상시, 계정·로그아웃은 확장으로. (`openSettings` 세션 유지)

### PWA 업데이트 반영 속도
- **서비스워커 네비게이션을 `cache: 'no-cache'` 재검증으로 변경**(`public/sw.js`, CACHE `cardfit-v1`→`v2`): GitHub Pages의 HTML `Cache-Control: max-age=600`(최대 10분) 때문에 배포 후에도 옛 버전이 잠깐 뜨던 문제 완화. 온라인이면 매 실행 시 origin에 ETag 재검증(변경 없으면 304)으로 최신 HTML을 받아옴. 오프라인 폴백은 그대로 유지. (앱 재설치 불필요 — 데이터 보존)

### 레이아웃
- **하단 탭바를 풀폭(edge-to-edge)으로 변경**: 기존 플로팅 알약형은 좌우·하단 여백으로 카드가 비쳐 산만했음. 화면 하단에 밀착한 풀폭 바(상단 hairline + 프로스티드 블러 + active 그라데이션 알약)로 전환해 헤더와 톤을 맞추고 가독성 개선. `.app-shell` 하단 패딩 축소, safe-area 반영. 동기화/기능 코드에는 영향 없음(CSS 전용). (src/styles.css)

### 클라우드 동기화 준비
- **Firebase 적용 계획서 추가**: 비로그인=localStorage 전용, 로그인=Firebase Auth + Firestore 동기화 구조를 전제로 `docs/FIREBASE_SYNC_PLAN.md` 작성. 사용자 Firebase 콘솔 설정 체크리스트, Firestore 데이터 모델, 보안 규칙 초안, 구현 단계, 확인 질문을 정리. README/ROADMAP에서 참조 연결.
- **Firebase Auth + Firestore 동기화 1차 구현**: `CardFit` Firebase 웹앱 설정(`cardfit-ee4b5`)을 연결하고, 설정/백업 탭에 Google 로그인, 동기화 상태, 수동 동기화, 로그아웃 UI 추가. 비로그인 localStorage 모드는 유지하고, 로그인 시 `users/{uid}/private/cardfit` 문서에 현재 상태 스냅샷을 저장/병합/구독.

### 추천 정확도 · 입력 개선
- **결제추천 조건 미충족 힌트**: 조건 때문에 혜택가치가 밀린 카드에 사유를 앰버 힌트로 표시(예: `최소 12,000원 이상 필요`, `전월실적 미달`). `scoreCard()`가 `conditions` 배열을 반환하고, `renderRankItem()`이 렌더링. 혜택가치 0(예: SKT 우리카드)이면 큰 힌트, 기본 소액 혜택은 있으나 일부 조건 미충족(예: The O)이면 작은(compact) 힌트를 함께 표시. 중복을 줄이려 설명문장의 `· 조건 확인:` 꼬리표는 제거. (src/lib/recommend.js, src/main.js)
- **금액 입력 천단위 콤마 통일**: 결제 예정 금액, 혜택 `이번달 사용금액` / `혜택 사용액·적립액`, 횟수형 `혜택금액` 입력을 `type="text" inputmode="numeric" data-money-input`으로 변경, 저장 시 콤마 제거 파싱(`replace(/[^\d]/g,'')`). 포인트 가치 입력은 소수점 값이라 제외.
- **혜택 메모**: 입력마다 전체 `render()`를 호출하던 것을 **blur 저장**으로 변경(모바일 커서/포커스 흔들림 방지). 숫자 필드는 기존대로 change+blur.
- **정렬 모드 탭 가드**: `state.isSortingCards`일 때 대시보드 카드 탭으로 상세 진입하지 않도록 차단(순서 변경에 집중).

### 실적 / 혜택 UI
- **대시보드 진행바 상태 색상화**: 달성=초록(발광), 부족=앰버, 관리없음=중립 회색. (카드사 브랜드색을 따라 회색으로 묻히던 달성 여부를 명확화)
- **카드상세 혜택 박스 접기/펼치기**: `<details>` 기반. 기본은 요약(혜택명 + 월/연/한도 라벨 칩)만, 탭하면 상세·입력·메모 노출. 세션 내 펼침 상태 유지(`openBenefits` Set — 값 입력 재렌더 시 접히지 않음).

### 리디자인 · 브랜딩 · PWA
- **CardFit 브랜딩**: 앱 이름·로고·파비콘·앱 아이콘(`public/icons/`).
- **설치형 + 오프라인 PWA**: `public/manifest.webmanifest`(standalone), `public/sw.js`(network-first), iOS 홈화면 메타. `public/`는 Vite 빌드 시 `dist/`로 자동 복사.
- **4개 화면 UI 리디자인**(카드현황/결제추천/카드상세/설정): 소프트 라운드, 카드사 브랜드 그라데이션(`theme-*` → `--card-accent`), 아이콘 하단 탭, 상단 요약 칩.
- 결제추천 실적 채우기 랭킹의 세로 깨짐·중복 표시 수정(부족액 배지).

## 참고: 데이터 저장 규칙
- localStorage 키 `cardBenefitManager.v1` **고정**. `migrateState()`가 기본값 위에 저장값을 병합하므로 앱을 업데이트해도 기존 입력값(실적·혜택 사용내역·메모·순서·포인트 가치)은 보존됩니다.
- **스키마를 크게 바꿀 때(키 변경 등)만 별도 마이그레이션이 필요**합니다. 그 외 새 카드/새 필드 추가는 병합으로 안전하게 보존됩니다.
