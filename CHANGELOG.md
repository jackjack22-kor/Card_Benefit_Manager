# 변경 이력 (Changelog)

사람이 읽는 변경 요약입니다. 상세 diff는 git 로그를 참고하세요.
**새 작업을 이어서 할 때는 이 파일 최상단에 최신 변경을 추가**해 항상 최신 기준을 유지하세요. (Codex/Claude 공용 기준 문서)

## 2026-07-09

### 카드 데이터 출처 구조화 기반
- **공개판 출처 상태 배지 추가**: 카드 상세 상단에 구조화된 `source.status` 기반 배지를 표시해 `official_verified`와 `needs_official_recheck` 상태가 사용자에게 다르게 보이도록 보강.
- **신한카드 재검증 배치 추적**: 신한 The ACE 블루라벨, 메리어트 본보이 더 베스트/더 클래식, 신한 Always On에 `shinhan-2026-07` 재검증 배치와 공식 진입점을 추가하되, 상품별 공식 원문 확인 전까지 `needs_official_recheck` 상태를 유지.
- **제휴 브랜드 모델 1차 확장**: 삼성 THE 1 스카이패스, 스카이패스 KB국민 플래티늄, 롯데 아멕스 스카이패스, GOAT BC 바로카드에 `networks`를 추가해 AMEX/Mastercard/VISA 서비스 차이를 담을 구조를 넓힘.
- **현대 아멕스 플래티넘 메타데이터 샘플 추가**: `hyundai-amex-platinum`에 구조화된 `source`와 `networks` 필드를 추가해, 공개판 확장 전 공식 재검증 필요 상태와 AMEX 단일 네트워크 모델을 코드에서 추적하도록 보강.
- **전체 카드 출처 메타데이터 필수화**: 현재 18장 모든 카드에 구조화된 `source` 필드를 추가해 기존 `sourceNote`와 별도로 공식 재검증 상태, 기준일, PDF/공식 사이트 단서를 추적하도록 정리.
- **출처 재검증 대기열 보고 추가**: `npm run source:report`를 추가해 카드사별 공식 재검증 대상을 `source` 상태, 확인일, 출처 단서, 재검증 메모와 함께 출력하도록 구성.
- **카드 데이터 감사 보강**: `npm run audit:check`에서 필수 `source`와 선택형 `networks` 필드의 상태값, 확인일 형식, 연회비 숫자값, 현대 아멕스 샘플 모델을 검증하도록 추가.

## 2026-07-06

### 공개 배포판 분리 기반
- **앱 에디션 분리**: `VITE_APP_EDITION` 기반으로 개인용(`personal`)과 공개용(`public`)을 구분하는 `appEdition` 설정을 추가. 개인용은 기존 Firebase 동기화와 `cardBenefitManager.v1` 저장 키를 유지하고, 공개용은 `cardfit.public.v1` 로컬 저장 키를 사용하도록 분리.
- **공개판 로컬 전용 처리**: 공개용 빌드에서는 클라우드 동기화 초기화/저장 요청을 no-op 처리하고, 설정 화면의 동기화 카드와 충돌 UI를 숨기도록 보강. 사용자가 입력한 설정/실적 데이터가 공개판에서 브라우저 로컬 데이터 기준으로만 유지되도록 정리.
- **Cloudflare Pages 빌드 추가**: `build:public`과 `tools/prepare-cloudflare-build.mjs`를 추가해 카드 이미지 복사, SPA `_redirects`, 기본 보안/캐시 `_headers`를 산출하도록 구성.
- **공개판 산출물 검증 추가**: `tools/verify-public-dist.mjs`와 `verify:public`을 추가하고 `build:public` 마지막에 연결해, 실제 `dist` 기준으로 Cloudflare 파일, 카드 이미지, 공개판 저장 키, `syncManager` 제외 여부를 검증.
- **감사 테스트 보강**: 공개판/개인판 빌드 모드, 저장 키 분리, 동기화 비활성화, Cloudflare 산출물 생성을 `npm run audit:check`에서 검증하도록 추가.
- **공개판 백업 UX 보강**: 공개판 설정 화면에 로컬 전용 데이터 보관 카드를 추가하고, JSON 백업 버튼이 여러 곳에 있어도 모두 동작하도록 이벤트 바인딩을 보강.
- **운영/리서치 문서 추가**: Cloudflare Pages 고정 도메인 운영 기준(`docs/PUBLIC_DISTRIBUTION_PLAN.md`)과 카드 데이터 공식 출처 검증 기준(`docs/CARD_DATA_RESEARCH_GUIDE.md`)을 추가.
- **카드 출처 매트릭스 추가**: 현재 18장 카드의 공식 재검증 상태, 브랜드 차이 확인 필요 항목, 카드사별 추가 후보군을 `docs/CARD_DATA_SOURCE_MATRIX.md`로 정리하고 감사 테스트에 카드 ID 추적을 추가.
- **카드 데이터 품질 게이트 추가**: `npm run audit:check`에서 카드/혜택 ID 중복, 필수 카드·혜택 필드, `image/clean` PNG 이미지 존재·가로형 크기, 주기/우선순위 값을 검증하도록 보강.
- **공개판 구현 마스터 플랜 추가**: 개인용/공개판 분리 원칙, 단계별 구현 순서, 카드 데이터 확장, 광고, 배포, 원복 방지 체크리스트를 `docs/PUBLIC_PRODUCT_IMPLEMENTATION_PLAN.md`로 정리하고 감사 테스트에 핵심 문서 연결을 추가.

### 사용자 입력값 기본값 원복 방지 전반 보강
- **카드 상세설정 필드 보호 확대**: 월 실적 목표뿐 아니라 연간 실적 목표, 연간 사용액 보정, 연회비 시작월, 주기 타입, 카드 메모에도 사용자 설정 플래그와 필드별 수정 시각을 기록하도록 보강.
- **동기화 병합 기준 개선**: `cardOverrides` 병합을 공통 필드 메타데이터 기반으로 확장해, 다른 기기의 기본값이 현대 아멕스 플래티넘 연간 목표/연회비 시작월 같은 사용자 최종 설정을 덮지 못하도록 처리.
- **사용 실적 병합 보강**: 월별 카드 사용액과 혜택 사용기록에 필드별 `UpdatedAt`을 남기고, 동기화 병합 시 가능한 경우 필드별 최신 입력을 우선하도록 보강.
- **기본값 보유 설정 보강**: 포인트 가치, 카드 순서, 숨김 카드 목록에도 수정 시각을 남겨 기본 상태를 가진 다른 기기의 최신 저장이 사용자 설정을 원복시키는 위험을 줄임.
- **회귀 테스트 추가**: 현대 아멕스 플래티넘의 연간 목표 3,600만원 및 연회비 시작월 변경값이 구형 저장 데이터 마이그레이션 후 사용자 설정으로 유지되는지 `npm run audit:check`에서 검증.

### 월 실적 목표 사용자 설정 원복 방지 보강
- **사용자 설정 플래그 추가**: 카드 상세설정에서 월 실적 목표를 바꾸면 `monthlyTargetUserSet`을 함께 저장해, 카탈로그 기본값과 사용자가 마지막으로 고른 값을 명확히 구분하도록 보강.
- **원격 스냅샷 병합 경로 보정**: Firebase 원격 데이터가 들어올 때 전체 state를 바로 덮어쓰지 않고 항상 `mergeStates()`를 통과하게 변경해, 오래된 원격 기본값이 로컬 사용자 설정을 밀어내는 경로를 차단.
- **메리어트 베스트 복구 처리**: 신한 메리어트 본보이™ 더 베스트에 남아 있던 기존 30만원 기본값 흔적은 `관리 안 함` 사용자 설정으로 복구되도록 마이그레이션 보정.
- **회귀 테스트 추가**: 명시적 `0원` 월 목표 유지, 메리어트 베스트 stale 기본값 복구, 일반 카드 카탈로그 기본값 비사용자 설정 판정, 원격 스냅샷 병합 경로를 `npm run audit:check`에서 검증.

### GitHub Pages 자동 배포 중단
- **Pages 자동 배포 비활성화**: Firebase Hosting 전환 이후 최신 운영 주소가 둘로 갈라지는 혼선을 막기 위해 `.github/workflows/pages.yml`의 `push main` 트리거를 제거하고, GitHub Pages 배포는 `workflow_dispatch` 수동 실행 전용 fallback으로 변경.
- **문서 정리**: README와 Firebase Hosting 전환 계획서에서 GitHub Pages를 자동 병행 운영이 아닌 수동 fallback으로 설명하도록 갱신.
- **감사 테스트 보강**: GitHub Pages workflow가 수동 실행만 남기고 `push` 자동 배포를 갖지 않는지 `npm run audit:check`에서 검증하도록 추가.

### 월 실적 목표 관리안함 동기화 원복 방지
- **월 실적 목표 필드 타임스탬프 추가**: 카드 상세설정에서 월 실적 목표를 변경할 때 `monthlyTargetUpdatedAt`을 기록해, `관리 안 함(0원)`처럼 기본값과 다른 명시 설정이 클라우드 병합 중 예전 기본 목표로 되돌아가지 않도록 보강.
- **클라우드 병합 보강**: `cardOverrides` 병합 시 전체 state의 `updatedAt`만 보지 않고 월 실적 목표는 필드 단위 변경 시각을 우선 비교하도록 변경. 메리어트 베스트처럼 기본 30만원이 있는 카드를 `관리 안 함`으로 바꾼 경우 원격의 오래된 30만원 값에 밀리지 않게 처리.
- **로그인 경계 저장 보존**: Firebase 런타임은 로드됐지만 현재 사용자가 아직 없는 상태에서도 데이터 변경을 pending cloud save로 남기도록 변경해, 로그인 복구 직전/직후 카드 설정 변경이 클라우드 저장 큐에서 빠지는 문제를 줄임.
- **의미 있는 카드 설정 판단 보강**: 월 실적 목표가 카드 기본값과 다르면 실사용 금액이 없어도 의미 있는 로컬 데이터로 간주해 첫 병합 때 원격 기본값에 쉽게 덮이지 않도록 개선.
- **회귀 테스트 추가**: 메리어트 베스트 `monthlyTarget: 0`과 `monthlyTargetUpdatedAt`이 마이그레이션 후에도 유지되는지, 동기화 병합 코드가 필드 단위 타임스탬프를 사용하는지 `npm run audit:check`에서 검증.

### Firebase Hosting 전환 준비
- **Hosting 설정 추가**: `.firebaserc`와 `firebase.json`을 추가해 기본 프로젝트를 `cardfit-ee4b5`로 고정하고, Firebase Hosting site/public 디렉터리, SPA rewrite, 서비스워커/HTML no-cache, hashed asset 장기 캐시 정책을 정의.
- **Hosting 전용 빌드 추가**: `build:hosting` 스크립트와 `tools/prepare-hosting-build.mjs`를 추가해 일반 Vite 번들을 Firebase Hosting에 배포하되, 앱이 참조하는 `image/clean` 카드 이미지는 `dist`로 복사되도록 구성.
- **GitHub Actions 배포 워크플로 추가**: `.github/workflows/firebase-hosting.yml`을 추가해 `main` push/수동 실행 시 audit과 Hosting 빌드를 수행하고, `FIREBASE_SERVICE_ACCOUNT_CARDFIT_EE4B5` secret이 준비된 경우 live Hosting 배포까지 진행. secret이 없으면 배포만 건너뛰도록 안전하게 구성.
- **전환 계획 문서화**: `docs/FIREBASE_HOSTING_MIGRATION_PLAN.md`를 추가해 목표 URL, 사용자 콘솔 설정, GitHub secret, Firestore rules 별도 배포, GitHub Pages 병행 전략, origin 변경 리스크, 검증 체크리스트를 정리.
- **README 갱신**: 권장 접속 주소를 Firebase Hosting 중심으로 갱신하되, GitHub Pages는 검증 기간 병행 운영으로 명시.
- **감사 테스트 보강**: Firebase Hosting site ID, cache headers, SPA rewrite, Hosting build script, GitHub Actions secret/deploy action이 유지되는지 `npm run audit:check`에서 검증하도록 추가.

### Firebase 동기화 자동 시작 및 저장 대기 보강
- **앱 시작 시 Auth 확인 자동화**: 설정 탭에 들어가야 Firebase 런타임이 켜지던 구조를 보강해, 앱 시작 직후 백그라운드에서 Google 로그인 상태를 확인하도록 변경.
- **런타임 미로드 저장 유실 방지**: Firebase 모듈이 아직 로드되지 않은 상태에서 사용자가 실적/혜택 값을 입력해도 `pendingCloudSave` marker를 localStorage에 남기고 동기화 런타임을 즉시 깨우도록 보강.
- **Auth 확인 전 저장 큐 보존**: Firebase 모듈은 로드됐지만 `onAuthStateChanged`가 아직 사용자 상태를 확정하기 전인 구간에서 저장 요청을 조용히 버리지 않고, 로그인 복구 후 flush 가능한 pending 상태로 남김.
- **Auth persistence 준비 추적**: `browserLocalPersistence` 설정 실패를 조용히 무시하지 않고 Promise로 추적하며, Google 로그인 요청은 persistence 준비 이후 진행하도록 정리.
- **회귀 테스트 추가**: lazy sync 부트스트랩, pending save marker, auth-settled guard, persistence 준비 대기를 `npm run audit:check`에서 소스 assertion으로 검증.

## 2026-07-05

### 삼성 THE 1 AMEX PLATINUM ELITE 혜택 보강
- **아티제/백미당 혜택 추가**: 삼성 THE 1 카드에 AMEX PLATINUM ELITE 공통 Dining 혜택인 `AMEX 아티제 커피 1+1`, `AMEX 백미당 커피·아이스크림 무료`를 추가. 월/연 횟수가 명확한 확정 혜택이므로 체크만으로 실사용 혜택 금액에 반영되도록 구성.
- **공항 식음료 할인 추가**: 스크린샷 기준 Travel 혜택의 `공항 내 식음료 매장 10~15% 할인`을 추가. 매장별 할인율이 달라 자동 금액 계산 대신 정보 확인/메모형 혜택으로 관리.
- **호텔 우대 설명 보강**: 기존 AMEX 호텔 우대 서비스에 구름에 3박 이상 1박 무료, 조선왕가호텔 10~20% 할인 문구를 포함해 삼성 THE 1 카드 앱/PDF 기준과 맞춤.
- **회귀 테스트 추가**: THE 1 아티제/백미당이 체크형 확정 혜택으로 계산되는지, 공항 식음료가 정보형 혜택으로 분류되는지 `npm run audit:check`에서 검증.

### iOS 상단 헤더-월바 틈 보정
- **iOS sticky gap 방어**: iOS Safari/PWA에서 상단 헤더와 sticky 월 이동바 사이에 투명 틈이 생겨 스크롤 내용이 비치는 문제를 줄이기 위해 헤더/월바 배경을 불투명 처리하고, 월바를 헤더 쪽으로 겹치게 배치.
- **배경막 추가**: sticky 월 이동바 상단에 pseudo-element 배경막을 추가해 iOS가 sticky 레이어 사이에 여백을 만들더라도 뒤 콘텐츠가 비치지 않도록 보정.
- **회귀 테스트 갱신**: 헤더 불투명 배경, 월바 겹침, iOS gap cover 구조를 `npm run audit:check`에서 검증.

### 후보 카드 3종 기본 숨김 추가
- **신규 카드 추가**: `GOAT BC 바로카드`, `ALL 우리카드 Infinite`, `힐튼 아너스 아멕스`를 카드 목록에 추가하고, 첨부 이미지를 가로형 clean PNG로 정규화해 반영.
- **기본 숨김 정책 추가**: 세 카드는 후보/관심 카드 성격이므로 `defaultHidden` 카드로 등록. 신규 설치와 기존 저장 데이터 마이그레이션에서 기본 숨김 처리하되, 사용자가 설정에서 표시로 바꾼 뒤에는 다시 자동 숨김 처리되지 않도록 보정.
- **포인트 가치 추가**: 페이북 머니 1원, 힐튼 아너스 1P=0.5원, ALL 리워드 1P=32.2원(공식 1P=0.02EUR 및 우리카드 안내의 2025년 평균 EUR 1,609.81원 기준)을 기본값으로 추가.
- **구간형 적립 공통 로직 추가**: GOAT 카드의 국내 100만원까지 1.5%/초과 1%, 해외 100만원까지 3%/초과 2%처럼 사용금액 구간별 적립률이 다른 혜택을 `amountTiers`로 계산하도록 공통화.
- **혜택 계산 검증 추가**: BC GOAT 국내/해외 페이북 머니, ALL 우리카드 기본/아코르 특별 적립, 힐튼 기본/특별 적립, 기본 숨김 마이그레이션을 `npm run audit:check`에 추가.

### KB 톡톡 my point 카드 이미지 교체
- **대표 이미지 갱신**: KB 톡톡 my point 카드의 clean 대표 이미지를 사용자가 제공한 카드 이미지로 교체.

### 신한 The ACE 블루라벨 혜택 순서 조정
- **스카이패스 최상단 배치**: 신한 The ACE 블루라벨 카드의 `스카이패스 마일리지 적립` 항목을 카드상세 혜택 목록 첫 번째로 이동.
- **회귀 테스트 추가**: 해당 카드의 첫 번째 혜택이 스카이패스 마일리지인지 `npm run audit:check`에서 검증.

### SKT 우리카드 통신요금 상태칩 정리
- **월 횟수칩 제거**: SKT 우리카드 `SKT 통신요금 자동납부 할인`에서 불필요한 `월 0/1회` 표시가 나오지 않도록 월 횟수 속성을 제거하고, `월한도 사용/한도` 상태만 보이게 정리.
- **회귀 테스트 추가**: 해당 혜택에 월 횟수 속성이 다시 붙지 않는지 `npm run audit:check`에서 검증.

### 통합 월 한도 혜택 순서 조정
- **통합 월 한도 최상단 배치**: 카드의정석 POINT 주거래의 `통합 월 적립한도`, MG+ S 하나카드의 `MG+ S 월 통합 할인한도`를 각 카드 혜택 상세 목록 첫 번째로 이동.
- **회귀 테스트 추가**: 두 카드의 통합 월 한도 항목이 첫 번째인지 `npm run audit:check`에서 검증.

### 월 이동바 헤더 밀착 보정
- **헤더-월바 간격 제거**: 상단 앱 헤더 하단 padding과 sticky 월 이동바의 top 기준/위쪽 여백을 더 줄여 카드현황·카드상세의 년월 이동 영역이 헤더 바로 아래에 붙도록 조정.
- **회귀 테스트 갱신**: compact header padding과 sticky month bar top/spacing 기준을 `npm run audit:check`에서 검증하도록 갱신.

### 월 이동바 간격 및 혜택 상태칩 정리
- **상단 월 이동바 간격 축소**: 카드현황/카드상세의 sticky 년월 이동 영역을 상단 헤더에 더 가깝게 붙도록 위쪽 margin/padding과 내부 높이를 축소.
- **금액형 혜택 상태칩 중복 제거**: 금액형 혜택에서 `이번달 30,000원`과 `월 3만/3만`처럼 같은 의미의 칩이 동시에 보이던 문제를 정리. 한도가 있으면 `월한도 사용/한도` 하나만, 한도가 없으면 `이번달 혜택액` 하나만 표시.
- **통합한도 기준 일원화**: 개별 혜택이 통합한도를 참조하는 경우 카드상세 상태칩도 통합한도 기준을 사용하도록 보정.

### 현대 아멕스 플래티넘 혜택 순서 조정
- **MR 적립 항목 최상단 배치**: 현대 아멕스 플래티넘 카드의 `기본 1.5MR`, `해외/국내 특급호텔 3MR`, `골프/면세점 4.5MR` 3개 항목을 혜택 상세 최상단으로 이동하고, `아티제 커피 1+1`은 4번째로 배치.
- **회귀 테스트 추가**: 현대 아멕스 플래티넘 상위 4개 혜택 순서를 `npm run audit:check`에서 검증.

### 메리어트 베스트 혜택 순서 조정
- **기본 1P/천원 위치 변경**: 신한 메리어트 본보이™ 더 베스트 카드의 `기본 1P/천원 적립` 항목을 클래식 카드와 동일하게 무료숙박권 바로 다음, 메리어트 5P 적립 위로 이동.
- **회귀 테스트 추가**: 베스트 카드에서 기본 1P 항목이 메리어트 5P 항목보다 앞에 위치하는지 `npm run audit:check`에 검증 추가.

### 혜택 상세 상태 배지 표시 보정
- **금액형 혜택 상태 배지 수정**: 자동 반영된 할인/적립금액이 있는데도 혜택 상세 우측 상단에 `이번달 미사용`으로 보이던 문제를 수정. 금액형 혜택은 계산된 이번달 혜택액을 `이번달 30,000원`처럼 표시.
- **통합한도형 상단 요약 보정**: MG+S 간편결제처럼 개별 혜택은 통합한도를 참조하는 구조에서 상단 요약이 `간편결제 0`으로 눌리던 문제를 수정하고 실제 적용 혜택액을 표시.
- **회귀 테스트 추가**: MG+S 간편결제 홈 상태와 금액형 상태 배지 정책을 `npm run audit:check`에 추가.

### 월 사용액 기반 상세 혜택 자동 반영 확대
- **기본 적립/마일리지 상세 행 자동 반영**: 이번달 사용액을 입력하면 카드상세의 기본 적립/마일리지 행에도 사용금액과 환산 혜택액이 채워지도록 변경. 대상: 현대 아멕스 플래티넘, 삼성 THE O/THE 1, 신한 The ACE 블루라벨, 신한 메리어트 클래식/베스트, SKYPASS 롯데 아멕스, 스카이패스 KB국민 플래티늄, 어디로든 그린카드, 쿠팡 와우카드 등.
- **정해진 사용 패턴 자동 배분**: SKT 우리카드는 이번달 사용액을 SKT 통신요금 자동납부 할인에, 카드의정석 POINT/KB 톡톡 my point/MG+S 하나카드는 간편결제 중심 사용 패턴에 자동 반영. 신한 Always On은 1만원 이상 2건 사용 패턴으로 자동 반영.
- **특별 적립은 기본 대비 추가분만 계산**: 해외/호텔/백화점 등 특별 적립처에 사용자가 입력한 금액은 기본 적립과 중복 산정하지 않고 추가 적립분만 실사용 혜택에 더하도록 변경. THE 1 특별 마일리지처럼 월 포인트 한도가 있는 경우 한도 적용 후 기본 적립분과의 차이를 계산.
- **통합 한도 표시 개선**: MG+S 통합 할인한도, 카드의정석 POINT 통합 적립한도 같은 한도 컨테이너는 직접 입력칸이 아니라 이번달 적용 합계 요약으로 표시.
- **수동 수정 안정화**: 자동 반영된 입력칸을 단순히 포커스/blur만 해도 수동값으로 굳어지는 문제를 방지. 사용자가 실제로 금액을 바꾼 경우에만 수동 금액 override로 저장하고, 0원 수동 수정도 의도대로 계산에 반영.
- **카드 데이터 정리**: 신한 The ACE 블루라벨은 스카이패스 선택 기준으로 정리하고 아시아나/마이신한포인트 항목을 제거. 신한 메리어트 본보이 더 클래식에는 기본 1P/천원 적립 항목을 추가.
- **회귀 테스트 확대**: `npm run audit:check`에 카드별 상세 행 자동 반영, 특별 적립 중복 방지, 통합 한도 합산, 수동 금액 override, cap container 읽기 전용 표시 검증을 추가.

### 정액형 혜택 입력 간소화
- **정액 체크형 정책 추가**: `count_amount` 혜택 중 `fixedBenefit` 기반 정액 혜택은 결제금액 입력 없이 사용 여부/횟수만 관리하도록 변경. 예: 삼성 THE O 스타벅스·아웃백·호텔 레스토랑·모범택시, 삼성 THE 1 스타벅스·택시·공항주차, SKT 우리 영화 할인.
- **금액 연동 혜택은 입력 유지**: 비율형(`rate`) 또는 금액 구간형(`fixedBenefitByAmount`) 혜택은 결제금액에 따라 혜택액이 달라지므로 결제금액 입력을 유지. 예: THE O CGV 50%, 삼성 THE 1 메가박스 구간 할인, SKT 우리 커피/패밀리레스토랑.
- **오래된 결제금액 값 방어**: 정액 체크형 혜택은 과거에 저장된 `usedAmount`가 최소금액보다 낮아도 체크/횟수 입력을 신뢰해 정액 혜택액을 계산하도록 보정.
- **체크박스 표시 보정**: 브라우저/폰트별로 체크 표시가 우측 아래로 치우치지 않도록 텍스트 체크 대신 CSS 체크 모양을 중앙에 그리도록 변경.
- **회귀 테스트 추가**: 정액 체크형 분류, 비율/구간형 제외, 오래된 낮은 결제금액 방어, 체크 표시 CSS 구조를 `npm run audit:check`에 추가.

### 카드상세 실적 금액 표시 및 월 이동 고정
- **월 이동 영역 sticky 처리**: 카드현황과 카드상세에서만 월 이동 스테퍼를 별도 `sticky-month-bar`로 분리해 상단 앱 헤더 아래에 고정. 카드현황의 `⋯` 정렬 메뉴도 같은 고정 영역에 포함.
- **카드상세 실적 요약 금액 보강**: 카드상세 상단 요약의 `전월실적`, `이번달 실적`에 상태만 표시하지 않고 실제 금액/목표금액을 함께 표시. 예: `780,000원 (충족)`, `705,000원 / 300,000원 (달성)`.
- **회귀 테스트 추가**: `npm run audit:check`가 sticky 월 이동 영역과 금액 포함 실적 문구 구조를 검사하도록 보강.

### 카드 숨김/표시 설정 추가
- **카드 설정 메뉴로 확장**: 설정 탭의 `카드 순서 편집`을 `카드 설정`으로 변경하고, 포인트 가치보다 위로 배치. 카드별 표시/숨김 토글과 위/아래 순서 변경을 한 화면에서 관리하도록 구성.
- **숨김 카드 화면 제외**: 숨김 처리한 카드는 카드현황, 카드상세 카드 선택 목록, 결제추천, 월 실적/혜택 합산 계산에서 제외. 카드 기록과 설정은 삭제하지 않고 보존.
- **백업/동기화 반영**: 숨김 카드 목록(`hiddenCardIds`)을 로컬 저장, JSON 백업/복원, Firestore 동기화/충돌 감지 대상에 포함.
- **회귀 테스트 추가**: 숨김 카드가 추천/합산 계산에서 제외되는지, 설정 화면용 전체 카드 순서는 유지되는지, 백업 복원 시 숨김 카드 ID가 정리되는지 검증.

### 혜택 체크박스 가독성 보정
- **기본 브라우저 체크박스 제거**: 카드상세 혜택의 `이번달 확인/사용 메모`, `현재 주기 사용 완료`, `1만원 이상 결제` 등 체크형 컨트롤에서 브라우저 기본 검은 체크박스를 쓰지 않도록 `appearance: none` 기반 커스텀 체크박스로 변경.
- **라이트/다크 선택 상태 명확화**: 미선택은 테마 표면색+회색 테두리, 선택은 초록 배경+흰 체크 표시로 통일해 라이트/다크 모두 선택 여부를 분명히 구분.
- **회귀 테스트 추가**: 체크형 혜택 컨트롤이 기본 체크박스 대신 커스텀 체크 표시를 쓰는지 `npm run audit:check`에 검증 추가.

### 카드상세 월별 실적 입력 섹션 제거
- **카드상세 중복 섹션 삭제**: 카드상세 화면의 `YYYY년 M월 실적 입력` 섹션을 제거. 전월실적 상태는 월별 사용액과 실적 목표로 자동 계산하고, 이번달 사용액은 카드현황 카드에서 입력하는 흐름으로 일원화.
- **상세설정 발급월 제거**: 연회비/혜택 주기 관리는 `연회비 시작월`만 남기고 유사 항목인 `발급월` 입력을 삭제.
- **관리 안 함(0원) 보존 보정**: 월 실적 목표를 `관리 안 함`으로 설정한 경우 카드 기본 목표값으로 되돌아가지 않도록 `monthlyTarget` 0을 명시값으로 처리.
- **legacy 전월실적 수동 상태 무시**: 과거 저장 데이터에 `prevMonthStatusOverride`가 남아 있어도 전월 사용액과 월 실적 목표 기준 자동 판정이 우선되도록 정리.
- **회귀 테스트 추가**: 메리어트 더 베스트 월 실적 목표 0원 설정, 전월실적 상태 선택 UI 제거, 발급월 설정 제거를 `npm run audit:check`에 추가.

### 전월실적 관리 안함 카드 표현 보정
- **전월실적 목표가 없는 카드 문구 분리**: `defaultMonthlyTarget/monthlyTarget`이 0인 카드는 카드현황에서 `전월실적 충족` 대신 `전월실적 무관`으로 표시하고, 카드상세 요약에서는 `관리 안함`으로 표시.
- **중립 배지 스타일 추가**: 실적 충족/미달/확인과 시각적으로 구분되도록 회색 중립 배지 톤을 추가.
- **회귀 테스트 추가**: KB 스카이패스 플래티늄처럼 목표 실적이 0인 카드가 전월실적 관리 대상으로 취급되지 않는지 검증.

### 전월실적 자동 판정 보정
- **월별 전월실적 상태 자동 계산**: 선택 월의 전월 `currentMonthSpend`가 입력되어 있으면 카드 목표 실적과 비교해 `충족/미달`을 자동 판정하도록 `getCardOverride` 기준을 보정. 예: 2026년 6월 화면에서 2026년 5월 신한 The ACE 블루라벨 사용액 780,000원이 입력되어 있으면 `전월실적 충족`으로 표시.
- **전월 실사용액 계산 기준 노출**: 전월실적이 자동 충족된 경우 `prevMonthSpend`도 실제 전월 사용액으로 넘겨 구간형 혜택/추천 계산이 카드 목표액이 아닌 실제 전월금액을 기준으로 동작하도록 보강.
- **명시적 수동 상태 보존**: 카드상세에서 전월실적 상태를 직접 선택한 경우 `prevMonthStatusOverride`를 저장해 사용자의 직접 판단을 유지.
- **회귀 테스트 추가**: 전월 사용액 기반 자동 충족/미달, 실제 전월금액 전달, 명시적 수동 override 보존 케이스를 `npm run audit:check`에 추가.

### 횟수형 혜택 사용 UX/혜택율 반영 보정
- **횟수형 혜택 컨트롤 재구성**: `count_amount` 혜택의 `- / + / 숫자 / 혜택금액 / 사용함` 한 줄 배치를 `사용 횟수`, `결제금액`, `예상 혜택`, `이번달 사용함` 구조로 정리. 모바일에서 의미 단위로 줄바꿈되고 라이트/다크 모두 선택 상태가 명확히 보이도록 스타일 보강.
- **사용 체크와 횟수/혜택금액 동기화**: `이번달 사용함`을 누르면 사용 횟수 1회와 예상 혜택금액이 함께 저장되고, 해제하면 횟수와 혜택금액이 0으로 정리되도록 변경. 직접 횟수를 수정해도 체크 상태와 혜택금액이 같이 갱신.
- **실사용 혜택율 반영 보정**: `count_amount` 혜택도 정액/구간정액/비율/월한도/연한도를 기준으로 월 실사용 혜택에 자동 반영. 예: 삼성 THE O 스타벅스 사용 체크 시 3,000원 반영, 아웃백 6만원 결제 시 30,000원 반영, CGV 12,000원 결제 시 6,000원 반영.
- **사용횟수 이중 카운트 방지**: `checked`와 `count`가 동시에 있어도 월/연 사용횟수를 2회로 세지 않도록 `getMonthlyUsageCount` 기준으로 통일. 상세 칩, 잔여 횟수, 연간 횟수, 추천 소진 판정이 같은 기준을 사용.
- **회귀 테스트 추가**: THE O 스타벅스/아웃백/CGV 케이스와 체크+count 이중 카운트 방지 케이스를 `npm run audit:check`에 추가.

### 구조 안정화 마무리
- **동시 수정 충돌 표시/해결 추가**: 클라우드 저장 시 마지막 동기화 기준 상태와 로컬/원격 변경을 비교해 같은 필드가 서로 다르게 바뀐 경우 `syncConflicts`로 보존. 설정 탭에서 내 값 유지 또는 원격 값 적용을 선택할 수 있도록 충돌 카드 UI를 추가.
- **Firebase 지연 로딩**: `main.js`의 Firebase 정적 import 경로를 제거하고 `lazySync.js`를 통해 설정 탭 진입/클라우드 액션 시점에만 Firebase Auth/Firestore 런타임을 불러오도록 변경. 검증 빌드 기준 초기 JS chunk가 약 794KB에서 약 116KB로 감소.
- **CSP 적용**: GitHub Pages/인라인 빌드 구조를 깨지 않는 범위에서 `index.html`에 meta CSP를 추가. Firebase Auth/Firestore에 필요한 도메인을 명시적으로 허용하고 `object-src`, `base-uri`, `frame-src`, `connect-src` 등을 제한.
- **main.js 책임 일부 분리**: 주요 업종, 원격 반영 시 보존할 UI 상태 키, 백업 파일 크기 제한을 `src/lib/ui/constants.js`로 분리. 큰 화면 렌더 함수는 회귀 위험을 고려해 유지.
- **정렬 동기화 누락 보정**: 드래그 기반 카드 순서 변경도 `persistState()` 경로를 타도록 수정해 클라우드 저장 큐에 반영.
- **회귀 방지 감사 추가**: `npm run audit:check`가 Firebase 런타임 동적 import, CSP meta 유지 여부를 함께 검사하도록 보강. 상세 내용은 `docs/STRUCTURAL_HARDENING_REPORT_2026-07-05.md`에 기록.

### 안정성/보안/성능 보강
- **Firebase 동기화 저장 보강**: 클라우드 저장 시 Firestore transaction으로 최신 원격 문서를 다시 읽고 병합한 뒤 revision을 올리도록 변경. 저장 실패/종료 시 재시도 힌트를 남기는 pending 플래그를 추가하고, 원격 상태 적용 중 예외가 나도 `savingRemote`가 고정되지 않도록 `try/finally` 처리.
- **명시적 0원 수동 고정 정책 추가**: `benefitValue: 0`만으로는 기존 자동계산을 유지하되, 사용자가 혜택금액을 직접 저장하면 `manualBenefitOverride`를 기록해 0원도 수동값으로 인정. 사용금액 입력으로 자동계산되는 경우에는 수동 고정을 해제.
- **전월실적 미달 추천 보정**: 조건부 `reward` 혜택도 전월실적 미달 시 추천 금액에서 제외. 단, `전월실적 관계없이` 문구가 있는 기본 적립은 제외하지 않도록 판별 로직을 분리.
- **카드현황 빠른 입력 추가**: 카드현황 카드 안에서 이번달 사용액을 바로 수정할 수 있도록 빠른 입력칸 추가. 기존 월별 저장 로직을 재사용해 카드상세와 같은 데이터에 저장.
- **백업/저장 예외 처리 강화**: localStorage 저장 실패를 사용자에게 알리고, 손상된 기존 로컬 데이터는 best-effort로 별도 키에 보존. 빈 JSON import 차단, 1MB 초과 백업 파일 차단, 파일 읽기 실패/중단 처리 추가. import 시 탭/카테고리/포인트키/월별 카드 ID/전월실적 상태를 정규화.
- **PWA 캐시 보수화**: 서비스워커 precache 실패를 조용히 삼키지 않도록 하고, 같은 origin 전체 GET 대신 문서/정적 asset/image/icon 중심으로 캐시 대상을 제한.
- **문서/규칙 추가**: `docs/FORMULAS.md`에 혜택 타입·자동 산식·추천 정책·감사 스크립트 기준을 문서화. `firestore.rules`를 추가해 사용자 본인 `users/{uid}/private/**`만 읽기/쓰기 허용하는 기준 규칙을 저장소에 보관.
- **감사 스크립트 확대**: `npm run audit:check`에 명시적 0원 override, 전월실적 미달 조건부 리워드 차단, 빈 백업 차단, import 정규화 케이스 추가. 누락됐던 `travel` 카테고리도 추가.

### Full Audit 보강
- **감사 보고서 추가**: `docs/FULL_AUDIT_2026-07-05.md`에 제품 효용성, 서브 에이전트 감사 결과, 적용한 수정, 남은 P1/P2/P3 리스크, 다음 로드맵을 정리.
- **결제추천 즉시 갱신**: 결제 예정 금액 입력 시 `change`를 기다리지 않고 추천 결과 영역만 즉시 재계산하도록 변경. 모바일 키보드/커서 흔들림을 줄이기 위해 입력창 전체 재렌더 대신 추천 결과 컬럼만 부분 갱신.
- **추천 산식 한도 보정**: `reward` 타입 추천 계산에서 `monthlyPointCap`을 반영해 삼성 THE 1 특별 마일리지처럼 월 적립한도가 있는 혜택이 큰 결제에서 과대 추천되지 않도록 보정.
- **혜택 상태칩 한도 표시 보정**: 카드 상세/현황의 혜택 홈 상태가 선택 월 사용액 기반 자동 산식과 어긋나지 않도록, 전월 추정 실적과 이번달 사용액 중 큰 값을 기준으로 월 한도 표시.
- **PWA 캐시 안정화**: 서비스워커가 GitHub Pages 배포 중 일시적 404/500 응답을 캐시에 저장하지 않도록 `response.ok` 응답만 캐시.
- **반복 감사 스크립트 추가**: `npm run audit:check`와 `tools/audit-check.mjs`를 추가해 SKT 우리카드, 카드의정석 POINT, KB 톡톡 my point, MG+S 하나, 신한 Always On, 쿠팡 와우, Marriott Classic, 삼성 THE 1 월 마일리지 한도 등 핵심 산식 골든 케이스를 자동 검증.

### 월 사용액 기반 실사용 혜택 자동 반영
- **기본 적립/마일리지 자동 환산**: `reward` 타입의 기본 적립 혜택은 이번달 카드 사용액만 입력해도 포인트/마일리지 기본 가치(`설정 > 포인트 가치`)로 원화 환산해 실사용 혜택에 자동 반영. 신한 The ACE 기본 마일리지에는 대한항공 1,500원당 1마일 기준을 추가.
- **고정 사용 패턴 자동 산출**: SKT 우리카드 통신요금 할인, 카드의정석 POINT 간편결제 3.8% 적립, KB 톡톡 my point KB Pay 5%+기본 0.5%, MG+S 하나 간편결제 10%, 신한 Always On 2건 2,000P는 카드 월 사용액 기준으로 자동 계산.
- **과대계상 방지**: 수동 입력값이 있으면 수동값을 우선하되, 0원 입력은 자동값을 막지 않도록 조정. 카드의정석/MG+S 통합한도 풀은 수동 입력값을 차감해 자동값과 합산해도 월 한도를 넘지 않게 보정. 전월실적 미달 상태에서는 실적 조건이 있는 자동 패턴을 0원 처리.
- **상세 표시 보강**: 카드 상세 상단 실사용 혜택률과 혜택 카드 상태칩도 자동 산출값을 읽도록 연결. 혜택별 직접 사용금액 입력 자동계산에는 최소 결제금액 조건을 반영.

### iPhone 입력값 되돌림 방지
- **월별 카드 실적 입력 저장 타이밍 보강**: 카드상세의 이번달 사용액은 `change` 이벤트만 기다리지 않고 `input`/`blur`에서도 로컬 상태와 localStorage에 즉시 반영. iOS Safari/PWA에서 숫자 입력 후 바로 하단 탭, 월 이동, 다른 카드 선택을 눌러도 입력값이 렌더 전에 유실되지 않도록 함.
- **화면 이동 직전 커밋 가드 추가**: 하단 탭, 월 스테퍼, 카드 현황 카드, 카드상세 카드 선택을 누르는 `pointerdown`/`touchstart`/`click` 시점에 현재 포커스된 월별 입력값을 한 번 더 저장. 모바일 터치 이벤트 순서 차이로 `change`가 늦게 발생하는 경우를 방어.
- **클라우드 동기화 덮어쓰기 보호**: 디바운스된 Firestore 저장은 캡처된 과거 state가 아니라 항상 최신 메모리 state를 기준으로 저장. 원격 스냅샷 수신 시 로컬 저장 대기값이 있거나 로컬 `updatedAt`이 더 최신이면 원격 상태와 병합해 적용하고, 페이지 숨김/종료 시 대기 중인 클라우드 저장을 즉시 flush 시도.

### 쿠팡 와우카드 보강
- **카드 이미지 반영**: 첨부 이미지를 `image/clean/coupang-wow-card.png`로 추가하고 쿠팡 와우카드에 연결.
- **쿠팡캐시 4% 적립 반영**: 쿠팡 와우카드는 월 실적 목표 없는 참고 카드로 유지하되, 이번달 사용액 입력 시 `쿠팡캐시 4% 적립`을 실사용 혜택률에 자동 반영. 결제추천에는 끼지 않도록 카테고리는 `other`로 유지.

## 2026-07-04

### 실사용 혜택률 보조지표 추가
- **월별 실사용 혜택률 표시**: 카드현황 카드별 섹션에 `실사용 혜택 n원 · n%` 보조지표를 작게 추가하고, 상단 `이번달 총 사용 금액` 요약칩에도 전체 실사용 혜택/종합 혜택률을 표시. 카드상세 상단 메트릭에도 선택 카드의 월별 실사용 혜택률을 추가.
- **계산 기준 명확화**: 선택월의 `benefitValue`/구버전 `usedBenefit`에 저장된 실제 할인·적립액만 합산. 연회비 쿠폰/기프트/라운지/안내형 등 주관적 가치가 큰 `check`, `info`, `info_check`, `milestone` 타입은 제외. 사용액이 0원이면 `-%` 대신 `-`로 표시해 `NaN%` 방지.
- **검증 보완**: `benefitValue: 0`이 저장된 경우 구버전 `usedBenefit` fallback으로 과대 합산되지 않도록 0값을 존중. 좁은 모바일 화면에서는 요약칩/상세 메트릭을 1열로 내려 보조지표 추가에 따른 줄바꿈 부담을 줄임.

### 참고용 카드 추가
- **쿠팡 와우카드 추가**: 쿠팡 결제 월 사용액 확인용 참고 카드로 추가. 월 실적 목표는 `관리 안 함`, 기본 정렬은 카드 목록 마지막. 공식 적립률/한도는 아직 추천 계산에 반영하지 않고, 카드상세에서 월 사용액을 기록하는 용도로만 구성.
- **목표 없는 카드의 월 사용액 표시 개선**: 카드현황에서 `관리 안 함` 카드도 `목표 없음` 대신 이번달 사용액을 표시하고 상태 문구를 `사용액 참고`로 보여주도록 변경.
- **쇼핑 세부 카테고리 추가**: 향후 쿠팡 결제추천 확장을 위해 `쇼핑 > 쿠팡/온라인쇼핑/기타 쇼핑` 세부 선택지를 추가.

### 카드 혜택 데이터 추가
- **스카이패스 KB국민 플래티늄카드 추가**: KB국민카드 공식 페이지 기준 MASTER 브랜드, 연회비 12만원, 대한항공 마일리지 1,500원당 1마일 적립, 연 1회 쿠폰서비스 중 `호텔 뷔페 무료 식사 이용권` 관리 항목을 추가. 호텔 뷔페 쿠폰은 결제추천 금액 랭킹에 과대 반영되지 않도록 `check` 타입의 연 1회 사용 관리 혜택으로 구성.
- **카드 이미지 추가**: 첨부 이미지를 `image/clean/kb-skypass-platinum.png`로 저장하고 새 카드 이미지로 연결.

### 월별 실적 저장 안정화 · 혜택 한도/자동계산 보강
- **월별 실적 값이 화면 이동 후 예전 값으로 되돌아갈 수 있는 동기화 타이밍 보정**: `saveState()`가 실제 저장된 `updatedAt` 포함 상태를 반환하도록 하고, 데이터 변경(`persistState`) 시 메모리 state에도 반영해 클라우드 저장 큐가 최신 타임스탬프를 사용하도록 수정. 반대로 탭 이동·월 이동·테마·상세설정 열림 같은 보기 상태는 `touch:false`로 저장해 데이터 병합 기준을 흔들지 않게 분리.
- **신한 The ACE 블루라벨 의료비 연 20만원 한도 반영**: 기존 데이터 정의에는 `annualCap: 200000`이 있었지만 추천 계산은 월 한도만 반영. `amount_cap`/`amount_cap_pool`/`reward_cap_pool` 추천 계산에 연간 잔여 한도를 반영하고, 연한도 소진 시 조건 힌트로 표시.
- **횟수형 혜택 연간 횟수 제한 반영**: `count_amount` 추천 계산에서 월 횟수뿐 아니라 연 횟수(`annualLimitCount`) 소진도 체크. 월 금액 한도도 기존 사용 혜택액을 차감해 남은 한도 기준으로 계산.
- **이번달 사용금액 입력 시 혜택액 자동계산**: 금액한도형 혜택에서 `이번달 사용금액`을 입력하면 `혜택 사용액/적립액`을 혜택률·월한도·연한도·통합한도 기준으로 자동 산출. 예: 더에이스 의료비 50만원 입력 → 10% 5만원이 아니라 월 3만원/연 20만원 잔여분 내 금액으로 자동 제한.
- **카드상세 한도 표시 강화**: 연간 금액 한도가 있는 혜택은 요약 칩과 남은 한도 영역에 월한도/연한도를 함께 표시.

### 동기화 규칙 수정 · 하단 바 재구조화 · 카드상세 정리
- **클라우드 동기화를 '값 입력 시에만'으로 제한**: 기존엔 `setState`(탭 이동·월 이동·카테고리·카드 선택 등 UI 변경)도 전체 state를 클라우드에 기록해, 한 기기의 화면 이동이 다른 기기로 미러링됐음. ① `setState`/테마 토글/상세설정 토글은 **로컬 저장만**(동기화 안 함), 데이터 변경(실적·혜택·카드순서·포인트·백업/복원)만 `queueCloudSave`. ② 원격 반영(`applyRemoteState`)은 데이터만 반영하고 **화면 이동/보기 상태(`UI_STATE_KEYS`)·테마는 이 기기 값 유지** → 다른 기기 조작이 화면을 바꾸지 않음. (src/main.js)
- **하단 탭바를 `position: fixed` → `sticky` + flex 레이아웃으로 재구조화**: iOS에서 짧은 페이지일 때 fixed 바가 문서 끝으로 떠오르던 버그를 근본 차단. `.app-shell`을 100dvh flex 컬럼으로, `.view`를 `flex:1`로 두어 짧은 페이지는 본문이 공간을 채우고, `.tabs`는 `sticky bottom:0`로 항상 바닥 고정(긴 페이지 스크롤 시도 유지). 실측: 짧은/긴 페이지 모두 바가 뷰포트 바닥(gap 0).
- **설정 '앱/저장 안내' 상시 표시**(아코디언 → 일반 카드) + 동기화 정책 안내 문구 추가.
- **카드상세 큰 카드 이미지 제거**: 상단 썸네일 그리드로 카드를 고르므로 상세 히어로의 대형 카드 이미지는 중복 → 삭제해 상세를 컴팩트하게.

### 설정·상단·하단 바 정리
- **상단 타이틀 영역 높이 통일**: 카드현황/카드상세 page-head 높이가 미묘하게 달라 탭 전환 시 흔들리던 문제. `.head-top` `min-height: 44px`(스테퍼만/스테퍼+⋯ 정렬), 설명문 `line-clamp: 2 + min-height`로 2줄 고정 → 두 페이지 page-head 140px로 정확히 일치.
- **하단 탭 라벨 '설정/백업' → '설정'**, 설정 페이지 타이틀도 '설정'으로 변경.
- **클라우드 동기화 카드 재구성**: 좌측으로 쏠린 4줄 → 상태 배지 + '지금 동기화'(로그아웃 시 'Google 로그인')를 **동일 크기 2열 한 줄**(`.sync-row`)로. 계정 정보는 하단 나열 대신 **다른 메뉴처럼 우측 '+' 확장**(`.account-expand`, 아코디언 스타일)으로 통일.
- **하단 탭바 iOS 부유 버그 강화 조치**: 짧은 페이지에서 콘텐츠가 뷰포트보다 작아 `position:fixed` 바가 문서 끝에 뜨던 iOS 버그(안드로이드 정상)에, `.app-shell`에 `min-height: 100dvh`(100vh 폴백) 추가 → 문서가 항상 뷰포트를 채워 바가 바닥에 고정. (fixed 바 부유의 표준 해결책)

### 카드상세 성능·밀도 개선
- **카드 선택 시 부분 렌더로 깜빡임 제거**: 기존엔 카드 선택도 전체 `render()`(헤더·13개 썸네일·상세 전부 재생성)라 깜빡였음. `selectCard()`가 `.detail-main`만 교체하고 picker의 active 클래스만 토글하도록 변경(`renderCardDetailMain` 분리, detail-main 전용 `bindDetailMainEvents(root)` 스코프 바인딩). 헤더·썸네일·탭 DOM은 그대로 유지.
- **불필요한 클라우드 동기화 방지**: 카드 "선택"은 순수 화면 이동이라 `selectCard()`가 localStorage 저장·`queueCloudSave`를 하지 않음(기존 `setState`는 매 선택마다 클라우드 저장을 큐잉했음). 실제 데이터 입력은 기존대로 저장·동기화.
- **카드 타일 밀도 상향**: `minmax(64px)` → `minmax(52px)`로 조정해 모바일에서 약 6열 3줄로 전체 표시(화면폭 넓으면 열 수 자동 증가).

### 카드상세 UX 보완
- **카드 목록을 반응형 그리드로 교체**: 가로 스크롤 썸네일 스트립 → `repeat(auto-fill, minmax(64px,1fr))` 그리드(`.card-picker`, 이미지 타일). 전체 카드가 스크롤 없이 보여 (좁은 화면 여러 줄, 넓은 화면 한 줄) 화면폭에 따라 줄 수 자동 조절. 스크롤 위치 리셋·오프스크린 선택 미표시 문제 해소.
- **월 스테퍼 위치 통일**: 카드상세도 카드현황과 동일하게 상단 좌측(`head-top`)에 배치.
- **페이지 타이틀/설명 추가**: 카드상세 상단에 "카드 상세" 제목·설명을 넣어 다른 탭과 통일.
- **하단 탭바가 짧은 페이지(설정 등)에서 위로 뜨던 문제 수정**: iOS Safari의 `position: fixed` + `backdrop-filter` 동일 요소 조합 버그로, 콘텐츠가 짧으면 바가 바닥에 고정되지 않고 문서 끝에 위치. `.tabs`의 `backdrop-filter`(프로스티드 블러)를 제거하고 불투명 배경(`var(--surface)`)으로 변경해 고정 보장. (데스크톱 Chromium은 정상이라 미재현되던 버그)

### 카드 혜택 데이터 보강
- **월 실적 목표 기본값 보정**: 신한 Always On 월 목표를 1만원, KB 톡톡 my point 월 목표를 20만원으로 설정. 기존 저장 데이터에서 두 카드가 `관리 안 함(0원)`이면 schema `2.0.1` 마이그레이션으로 새 기본 목표를 1회 반영.
- **현대 아멕스 플래티넘 메탈 플레이트 항목 제거**: 이미 발급 완료된 일회성 안내 항목이라 카드상세 혜택 목록에서 삭제.
- **삼성 THE 1 AMEX PLATINUM ELITE 혜택 재정리**: `2604_AMEX_E_002.pdf` 기준으로 삼성카드 기본 혜택과 AMEX 등급서비스를 분리. AMEX 무료 주차, 호텔 발레파킹, 공항 발레파킹, 공항 라운지, 공항 커피, 공항 리무진·철도, 공항 Books Store 할인, AMEX 호텔/쇼핑/라이프스타일 우대 서비스를 확정 조건으로 보강. PDF 표에서 삼성 THE 1은 Be My Guest 미제공(`×`)이므로 THE 1에는 추가하지 않음.
- **결제추천 세부 매칭 보정**: 공항 커피 세부 카테고리를 추가하고, 주차 세부 선택 시 `인천공항/공항 발렛/호텔 발렛`이 섞이지 않도록 parking broad 매칭을 축소.
- **현대 아멕스 플래티넘 Amex Member 혜택 추가**: 아티제 커피 1+1, 백미당 커피·아이스크림 무료, Be My Guest 레스토랑/호텔 델리 할인, 국내 특급호텔·고택 호텔 할인, 국내 특급호텔 발레파킹, 공항 리무진·철도 무료, 공항 발레파킹, The Hotel Collection, Fine Hotels + Resorts를 추가. 카드상세 노출 순서는 사용 빈도 기준으로 아티제 → 백미당 → Be My Guest를 상단에 두고, 공항 리무진/발렛은 하단, THC/FHR은 최하단에 배치.
- **삼성 THE 1 누락 혜택 추가**: 삼성카드 앱 캡처 기준 무료 주차, SFC 놀이공원 우대, 발렛파킹, 메가박스 할인, 아웃백 10% 현장할인, 공항 커피, 공항 라운지 서비스를 추가. 금액 조건이 확인된 메가박스/아웃백은 결제추천에 반영하고, 대상처·조건 추가 확인이 필요한 공항/주차/라운지 서비스는 카드상세 관리 중심으로 반영.
- **세부 카테고리 보강**: 결제추천 정확도를 위해 커피(아티제·백미당), 호텔(FHR·THC·호텔 델리), 다이닝(아웃백·호텔 레스토랑·호텔 델리), 주차(공항/호텔 발렛) 세부 선택지를 추가.

### UX 개편 (레이아웃·정보 밀도)
- **하단 탭 순서 변경**: 카드현황 · 카드상세 · 결제추천 · 설정/백업.
- **헤더에서 월 이동 바 제거** → 헤더는 브랜드+테마토글만. 월은 카드현황/카드상세 상단의 **컴팩트 월 스테퍼**(`renderMonthStepper`, `‹ 2026년 7월 ›` + 이번달 아닐 때 '오늘')로 이동. 결제추천/설정에는 미표시.
- **카드현황 '정렬' → ⋯ 오버플로 메뉴**(`renderOverflowMenu`, `<details>` 드롭다운)로 이동해 공간 절약. 제목은 "카드 현황"으로.
- **요약 칩 재구성**: 좌측 `이번달 총 사용 금액`(전체 카드 currentMonthSpend 합계), 우측 `채울 실적 합계` = 금액 + 하단 `채운/관리 카드 수 달성`.
- **결제추천 카테고리/세부 카테고리 더보기**: 주요 7개(커피·대중교통·영화·백화점·의료·간편결제·해외)만 기본, 나머지는 더보기 확장. 세부는 상위 3개+더보기. (`categoryExpanded`/`subcategoryExpanded` 세션 유지)
- **카드상세 카드 목록 → 가로 썸네일 스트립**(`.card-strip`): 세로 목록 패널을 한 줄 스크롤 썸네일로 축소, 활성 카드 강조.
- **설정/백업 아코디언화**(`renderSettingsSection`, `<details>`): 포인트 가치·카드 순서·백업/복원·앱 안내를 접기. `백업 권장` 상시 문구 삭제. `데이터 이동 순서`는 백업/복원 하단으로 통합. **클라우드 동기화(코덱스)는 로직/핸들러 보존**하고 상태+지금 동기화만 상시, 계정·로그아웃은 확장으로. (`openSettings` 세션 유지)

### PWA 업데이트 반영 속도
- **서비스워커 네비게이션을 `cache: 'no-cache'` 재검증으로 변경**(`public/sw.js`, CACHE `cardfit-v1`→`v2`): GitHub Pages의 HTML `Cache-Control: max-age=600`(최대 10분) 때문에 배포 후에도 옛 버전이 잠깐 뜨던 문제 완화. 온라인이면 매 실행 시 origin에 ETag 재검증(변경 없으면 304)으로 최신 HTML을 받아옴. 오프라인 폴백은 그대로 유지. (앱 재설치 불필요 — 데이터 보존)

### 레이아웃
- **하단 탭바를 풀폭(edge-to-edge)으로 변경**: 기존 플로팅 알약형은 좌우·하단 여백으로 카드가 비쳐 산만했음. 화면 하단에 밀착한 풀폭 바(상단 hairline + 프로스티드 블러 + active 그라데이션 알약)로 전환해 헤더와 톤을 맞추고 가독성 개선. `.app-shell` 하단 패딩 축소, safe-area 반영. 동기화/기능 코드에는 영향 없음(CSS 전용). (src/styles.css)

### 클라우드 동기화 준비
- **Firebase 적용 계획서 추가**: 비로그인=localStorage 전용, 로그인=Firebase Auth + Firestore 동기화 구조를 전제로 `docs/FIREBASE_SYNC_PLAN.md` 작성. 사용자 Firebase 콘솔 설정 체크리스트, Firestore 데이터 모델, 보안 규칙 초안, 구현 단계, 확인 질문을 정리. README/ROADMAP에서 참조 연결.
- **Firebase Auth + Firestore 동기화 1차 구현**: `CardFit` Firebase 웹앱 설정(`cardfit-ee4b5`)을 연결하고, 설정/백업 탭에 Google 로그인, 동기화 상태, 수동 동기화, 로그아웃 UI 추가. 비로그인 localStorage 모드는 유지하고, 로그인 시 `users/{uid}/private/cardfit` 문서에 현재 상태 스냅샷을 저장/병합/구독.

### 추천 정확도 · 입력 개선
- **결제추천 조건 미충족 힌트**: 조건 때문에 혜택가치가 밀린 카드에 사유를 앰버 힌트로 표시(예: `최소 12,000원 이상 필요`, `전월실적 미달`). `scoreCard()`가 `conditions` 배열을 반환하고, `renderRankItem()`이 렌더링. 혜택가치 0(예: SKT 우리카드)이면 큰 힌트, 기본 소액 혜택은 있으나 일부 조건 미충족(예: The O)이면 작은(compact) 힌트를 함께 표시. 중복을 줄이려 설명문장의 `· 조건 확인:` 꼬리표는 제거. (src/lib/recommend.js, src/main.js)
- **금액 입력 천단위 콤마 통일**: 결제 예정 금액, 혜택 `이번달 사용금액` / `혜택 사용액·적립액`, 횟수형 `혜택금액` 입력을 `type="text" inputmode="numeric" data-money-input`으로 변경, 저장 시 콤마 제거 파싱(`replace(/[^\d]/g,'')`). 포인트 가치 입력은 소수점 값이라 제외.
- **혜택 메모**: 입력마다 전체 `render()`를 호출하던 것을 **blur 저장**으로 변경(모바일 커서/포커스 흔들림 방지). 숫자 필드는 기존대로 change+blur.
- **정렬 모드 탭 가드**: `state.isSortingCards`일 때 대시보드 카드 탭으로 상세 진입하지 않도록 차단(순서 변경에 집중).

### 실적 / 혜택 UI
- **대시보드 진행바 상태 색상화**: 달성=초록(발광), 부족=앰버, 관리없음=중립 회색. (카드사 브랜드색을 따라 회색으로 묻히던 달성 여부를 명확화)
- **카드상세 혜택 박스 접기/펼치기**: `<details>` 기반. 기본은 요약(혜택명 + 월/연/한도 라벨 칩)만, 탭하면 상세·입력·메모 노출. 세션 내 펼침 상태 유지(`openBenefits` Set — 값 입력 재렌더 시 접히지 않음).

### 리디자인 · 브랜딩 · PWA
- **CardFit 브랜딩**: 앱 이름·로고·파비콘·앱 아이콘(`public/icons/`).
- **설치형 + 오프라인 PWA**: `public/manifest.webmanifest`(standalone), `public/sw.js`(network-first), iOS 홈화면 메타. `public/`는 Vite 빌드 시 `dist/`로 자동 복사.
- **4개 화면 UI 리디자인**(카드현황/결제추천/카드상세/설정): 소프트 라운드, 카드사 브랜드 그라데이션(`theme-*` → `--card-accent`), 아이콘 하단 탭, 상단 요약 칩.
- 결제추천 실적 채우기 랭킹의 세로 깨짐·중복 표시 수정(부족액 배지).

## 참고: 데이터 저장 규칙
- localStorage 키 `cardBenefitManager.v1` **고정**. `migrateState()`가 기본값 위에 저장값을 병합하므로 앱을 업데이트해도 기존 입력값(실적·혜택 사용내역·메모·순서·포인트 가치)은 보존됩니다.
- **스키마를 크게 바꿀 때(키 변경 등)만 별도 마이그레이션이 필요**합니다. 그 외 새 카드/새 필드 추가는 병합으로 안전하게 보존됩니다.
