{
  "name": "card-benefit-manager",
  "version": "1.0.0-v1-codex",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:single": "vite build && node tools/build-single.mjs",
    "build:pages": "npm run build:single && node tools/build-pages.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check src/main.js && node --check src/data/cards.js && node --check src/data/categories.js && node --check src/lib/cycles.js && node --check src/lib/storage.js && node --check src/lib/recommend.js && node --check src/lib/format.js",
    "build:zip": "node tools/make-zip.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
